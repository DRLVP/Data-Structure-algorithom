#include <iostream>
using namespace std;

int main()
{
    // int a, num;
    // cout << "enter the number of num: ";
    // cin >> num;

    // POST INCREMENT
    // a = num++;
    // cout << "the post Increment value: " << a << endl;
    // cout << "the value of num is: " << num;

    // PRE INCREMENT
    // a = ++num;
    // cout << "the post Increment value: " << a << endl;
    // cout << "the value of num is: " << num;

    // cout << 12.5 / 2;
    // cout << 12 / 2.5;

    // *************** COMPARISION OPERATOR ************
    int a, b, c;
    a = 5, b = 4, c = 2;
    // // cout << (a > b > c);
    // // cout << (a > b < c);
    // cout << (5 != 4 > 2);

    // *************** LOGICAL OPERATOR ************

    if (a || c <= b)
    {
        cout << "allright \n";
    }
    else
    {
        cout << "not okey \n";
    }
}