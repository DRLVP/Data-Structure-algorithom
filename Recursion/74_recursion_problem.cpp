#include <iostream>
using namespace std;

// int findFactorial(int num)
// {
//     // base case
//     if (num == 1 || num == 0)
//         return 1;
//     return num * findFactorial(num - 1);
// }

// int sumOfNum(int n)
// {
//     // Base Case
//     if (n == 1)
//         return 1;

//     return n + sumOfNum(n - 1);
// }
/*
int pow(int num, int n)
{
    // Base Case
    if (n == 0)
        return 1;

    return num * pow(num, n - 1);
}
*/

// int sqSum(int n)
// {
//     // Base case
//     if (n == 1)
//         return 1;

//     return n * n + sqSum(n - 1);
// }

int main()
{
    /*
    // FIND FACTORIAL OF N
    int num;
    cout << "Enter the value of num: ";
    cin >> num;

    // handle negative number
    if (num < 0)
    {
        cout << "Factorial is not possible";
        return 0;
    }

    cout << findFactorial(num);
    */

    /*
     // SUM FIRST NATURAL NUMBER
     int num;
     cout << "Enter of value num: ";
     cin >> num;

     if (num <= 0)
     {
         cout << "Please enter positive number";
         return 0;
     }

     cout << sumOfNum(num);
     */
    /*

    // POWER OF 2 OF N TIME
    int num, N;
    cout << "Enter the value of num: ";
    cin >> num;
    cout << "Enter the value of N time: ";
    cin >> N;

    // handle for negetive number
    if (num <= 0 || N <= 0)
    {
        cout << "Please enter positive number";
        return 0;
    }

    cout << pow(num, N);
    */

    /*
     // SQUARE SUM OF N NATURAL NUMBER
     int num;
     cout << "Enter the value of num: ";
     cin >> num;
     if (num <= 0)
     {
         cout << "Please enter at least 1";
         return 0;
     }

     cout << sqSum(num);
     */
}